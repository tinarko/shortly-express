var db = require('../db');
var util = require('../lib/utility');

// Write you session database model methods here

module.exports = {
  // session: function(username, hash, callback) {
  //   db.query('SELECT id FROM users where username = ?', username, function(error, results) {

  //   });
  // }


};
